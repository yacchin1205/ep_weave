version: '3'

volumes:
  postgres_data_vol:
    external: false
  solr_data_vol:
    external: false

services:
  postgresql:
    image: postgres:15
    environment:
      POSTGRES_DB: etherpad
      POSTGRES_USER: etherpaduser
      POSTGRES_PASSWORD: etherpadpass
    volumes:
      - postgres_data_vol:/var/lib/postgresql/data/
    restart: always

  solr:
    build:
      context: .
      dockerfile: solr/Dockerfile
    volumes:
      - solr_data_vol:/var/solr/
    healthcheck:
      test: ["CMD", "curl", "-sf", "http://localhost:8983/solr/pad/admin/ping"]
      interval: 10s
      timeout: 5s
      retries: 5
      start_period: 60s
    restart: always

  etherpad:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - 9001:9001
    environment:
      DB_HOST: postgresql
      DB_NAME: etherpad
      DB_PASS: etherpadpass
      DB_PORT: 5432
      DB_TYPE: postgres
      DB_USER: etherpaduser
      LOGLEVEL: "debug"
    volumes:
      - ./demo/settings.json:/opt/etherpad-lite/settings.json:ro
    depends_on:
      postgresql:
        condition: service_started
      solr:
        condition: service_healthy
    restart: always
